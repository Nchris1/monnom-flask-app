pipeline {
    agent any

    stages {

        stage('Clonage') {
            steps {
                git branch: 'main', url: 'https://github.com/Nchris1/monnom-flask-app.git'
            }
        }

        stage('Build Image') {
            steps {
                sh 'docker build -t christelle-flask-app .'
            }
        }

        stage('Publication') {
            steps {
 sh 'docker login -u chrisnto -p Christelle'
                sh 'docker tag christelle-flask-app chrisnto/christelle-flask-app'
                sh 'docker push chrisnto/christelle-flask-app'
            }
        }
    }
}
